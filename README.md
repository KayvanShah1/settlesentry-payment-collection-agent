# SettleSentry: Payment Collection Agent
A policy-governed AI payment collection agent with strict identity verification, safe tool calls, and deterministic payment flow control.

## Quickstart (uv)

```bash
# from repo root
uv sync --all-packages
```